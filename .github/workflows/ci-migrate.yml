name: CI - Run Liquibase

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

env:
  DB_PASS: core

jobs:
  migrate:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        ports:
          - 5432:5432
        env:
          POSTGRES_DB: ${{ secrets.DB_NAME }}
          POSTGRES_USER: ${{ secrets.DB_USER }}
          POSTGRES_PASSWORD: core
        options: >-
          --health-cmd="pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Liquibase
        run: |
          curl -sLo liquibase.zip https://github.com/liquibase/liquibase/releases/download/v4.29.2/liquibase-4.29.2.zip
          unzip -o liquibase.zip -d liquibase-cli
          sudo mv liquibase-cli/liquibase /usr/local/bin/liquibase
          sudo mv liquibase-cli/internal /usr/local/bin/internal
          sudo chmod +x /usr/local/bin/liquibase
          liquibase --version

      - name: Run Liquibase migrations
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASS: core
        run: |
          liquibase \
            --url="jdbc:postgresql://$DB_HOST:$DB_PORT/$DB_NAME" \
            --username="$DB_USER" \
            --password="$DB_PASS" \
            --changeLogFile=liquibase/db.changelog-master.xml \
            update
